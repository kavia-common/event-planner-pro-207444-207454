export const environment = {
  production: false,
  /**
   * Base URL for the backend REST API.
   *
   * NOTE: Set this at build/deploy time via your hosting environment by replacing this file
   * or by updating it in a future step to use runtime config. For now we default to same-origin
   * with `/api` proxy behavior.
   */
  apiBaseUrl: '/api',
};
