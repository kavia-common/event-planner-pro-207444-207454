export const environment = {
  production: true,
  /**
   * Base URL for the backend REST API.
   * In production this is typically the same origin reverse-proxied to `/api`.
   */
  apiBaseUrl: '/api',
};
